export type BlogType = {
  id: string;
  title: string;
  subTitle: string;
  body: string;
  readDuration: number;
  image: string;
  createdAt: string;
  updatedAt: string;
};

export type BlogCategoryType = {
  id: string;
  name: string;
};